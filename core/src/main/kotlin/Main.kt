/**
 * Placeholder entry point for the `core` module.
 *
 * The game logic lives in the domain, application, and engine packages so the
 * module can be reused as a library by different front ends. This function
 * intentionally does nothing and exists only so the module remains runnable
 * when needed during local experiments.
 */
fun main() = Unit
